fastapi==0.141.1
httpx==0.28.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
cryptography==50.0.1
lxml==6.1.3
pywin32==310; platform_system == "Windows"
# Python zoneinfo needs this pinned database inside the PyInstaller one-file
# bundle so Windows does not depend on system IANA timezone data.
tzdata==2026.4
pytest==9.1.1
starlette==1.6.0